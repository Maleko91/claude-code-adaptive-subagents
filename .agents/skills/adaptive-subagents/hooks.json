{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Task",
        "type": "prompt",
        "prompt": "A subagent Task just completed. Mandatory steps — do both NOW:\n1. If you did not already output '> Routed to {Model}: {reason}' for this delegation, output it now.\n2. Append a row to the routing-log.md file: | {timestamp} | {task description} | {model} | {category} | {estimated saving} |"
      }
    ]
  }
}
